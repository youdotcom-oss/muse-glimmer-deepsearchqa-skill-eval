import { run as citationFidelity } from './citation-fidelity.ts'
import { run as contentsTruncationContext } from './contents-truncation-context.ts'
import { run as paramCompliance } from './param-compliance.ts'
import { run as queryEvolution } from './query-evolution.ts'
import { run as readDifficulty } from './read-difficulty.ts'
import { run as readDiscipline } from './read-discipline.ts'
import { run as roundProfile } from './round-profile.ts'
import { run as searchBooleanOperators } from './search-boolean-operators.ts'
import { run as searchCountDetail } from './search-count-detail.ts'
import { run as searchCountRound } from './search-count-round.ts'
import { run as searchParams } from './search-params.ts'
import { run as searchQueryCompliance } from './search-query-compliance.ts'
import { run as searchReadCoupling } from './search-read-coupling.ts'
import { run as sourceDiversity } from './source-diversity.ts'
import { run as toolBudget } from './tool-budget.ts'
import { run as zeroResultRecovery } from './zero-result-recovery.ts'

const HEADER = `# Tool & skill usage analysis

Generated by "bun run analysis" over local data/*.jsonl artifacts (not committed; download with "bun run download"). These are example analysis queries measuring how the evaluated model + Pi harness + You.com MCP you-search/you-contents tools + you-web skill combination actually used the tool surface, cross-referenced against the skill's guidance. Findings are intended to inform a future auto-research extension; that extension is out of scope here.

Each section is a self-contained, re-runnable ClickHouse query. Queries over data/results.jsonl (flat, fast) answer aggregate-budget questions; queries over data/graded.jsonl (raw, includes trial.trajectory) answer tool-parameter, citation, and source-diversity questions. Sections labeled "a" or "b" are deeper follow-ups into the preceding direction. Each script under analysis/ is also independently runnable, e.g. "bun run analysis/tool-budget.ts".

`

const directions = [
  toolBudget,
  readDiscipline,
  readDifficulty,
  paramCompliance,
  searchCountDetail,
  searchCountRound,
  searchParams,
  searchQueryCompliance,
  searchBooleanOperators,
  contentsTruncationContext,
  citationFidelity,
  sourceDiversity,
  roundProfile,
  queryEvolution,
  searchReadCoupling,
  zeroResultRecovery,
]

process.stdout.write(HEADER)
for (const run of directions) await process.stdout.write(await run())
